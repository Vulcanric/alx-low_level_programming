#  -*- Makefile -*-
#  Builds an executable: `school`

CC = gcc		# Compiler to use
SRC = main.c school.c	# Source files to compile
OBJ = $(SRC:.c=.o)	# Generate object file names from SRC using pattern substitution
NAME = school		# Name of the executable

# Rules for building
# Recompiles only when needed (only updated source files)
#

all: $(OBJ)	# Compiles object files into executable
	$(CC) $^ -o $(NAME)
